asyncapi: '2.6.0'
info:
  title: Simple API
  version: '1.0.0'
channels:
  test/channel:
    subscribe:
      message:
        payload:
          type: object
          properties:
            hello:
              type: string
