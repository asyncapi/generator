import { Text } from '@asyncapi/generator-react-sdk';
export function ClientClass({ clientName, serverUrl, title, sendOperations }) {  // Required props
  return (
    <Text>
      {/* Class definition for Kafka  client named {clientName} to interact with the AsyncAPI service titled "{title}". */} 
    </Text>
  );
}
