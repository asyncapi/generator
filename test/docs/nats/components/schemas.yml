lightMeasuredPayload:
  type: object
  $id: lightMeasuredPayload
  x-parser-schema-id: lightMeasuredPayload
  properties:
    lumens:
      type: integer
      minimum: 0
      description: Light intensity measured in lumens.
      x-pi: false
    sentAt:
      $ref: "#/sentAt"
turnOnOffPayload:
  type: object
  $id: turnOnOffPayload
  x-parser-schema-id: turnOnOffPayload
  properties:
    command:
      type: string
      enum:
        - on
        - off
      description: Whether to turn on or off the light.
      x-pi: false
    sentAt:
      $ref: "#/sentAt"
dimLightPayloadserere:
  type: object
  $id: dimLightPayloadserere
  x-parser-schema-id: dimLightPayloadserere
  properties:
    percentage:
      type: integer
      description: Percentage to which the light should be dimmed to.
      minimum: 0
      maximum: 100
    sentAt:
      $ref: "#/sentAt"
sentAt:
  type: string
  format: date-time
  description: Date and time when the message was sent.
response:
  type: object
  $id: response
  x-parser-schema-id: response
  properties:
    status:
      type: integer
      description: Like http codes
    statusMessage:
      type: string
      enum:
        - OK
        - ERROR
    sentAt:
      $ref: "#/sentAt"
