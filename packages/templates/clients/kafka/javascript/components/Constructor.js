import { Text } from '@asyncapi/generator-react-sdk';

export function Constructor({ serverUrl, sendOperations }) {

  return (
    <Text indent={2}>
     {/* Constructor to initialize the Kafka  client with server URL and send operations */}
    </Text>
  );
}
