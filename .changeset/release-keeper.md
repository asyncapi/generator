---
"@asyncapi/keeper": minor
---

Initial release of `@asyncapi/keeper` to make it available for JS templates in `@asyncapi/generator`. The generator release will contain a new JS client version with enabled message validation.
